#![allow(unused)]

fn main() {
    // strings in rust there are multiple ways of calling a string in rust
    let s: String =  String::from("I beleive i can fly");
    let s2: String = "im sure i can fly".to_string();
    let length:usize = s2.len(); 
}
